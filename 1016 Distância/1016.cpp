#include <iostream>
#include <stdio.h>
#include <cmath>

using namespace std;

int main() {
    int km;
    scanf("%d", &km); //Lendo o valor digitado pelo usuario
    printf("%d minutos\n", (km*(60/30)));

    return 0;
}
